{
  "name": "blackat",
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install && concurrently \"npm run setup:server\" \"npm run setup:client\" \"npm run setup:import\"",
    "setup:client": "cd client && npm install",
    "setup:server": "cd server && npm install",
    "setup:import": "cd import && npm install",
    "build": "cd client && npm run build",
    "start": "npm run server",
    "test": "concurrently \"npm run test:server\" \"npm run test:client\" \"npm run test:import\"",
    "test:client": "cd client && npm run test",
    "test:server": "cd server && npm test",
    "test:import": "cd import && npm test",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "import": "cd import && npm start",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
